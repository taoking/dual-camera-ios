# 变更记录

## Unreleased — 2026-07-27

### 新增

- 双摄照片的画中画、左右分屏、上下分屏与 3:4／1:1／9:16 比例。
- 画中画拖动、吸附、尺寸档位，及布局／镜像／网格／倒计时等持久化设置。
- 合成照片分享、成片加原图的可选保存模式、相册权限“去设置”入口。
- 后摄对焦／测光、缩放、倒计时、触感反馈、硬件诊断和 Fake Camera Mode。
- `CameraViewModel`、布局引擎、照片合成、相册服务、OSLog 与单元测试目标。
- `CameraFormatSelector`、`CameraSessionConfigurator`、照片／视频 Coordinator、生命周期／授权／运行期监视和诊断模块。
- 可注入 PhotoKit、UserDefaults 适配，Fake Camera UI Test Target 及自适应模拟器 GitHub Actions。
- `MediaSaveCoordinator`：以照片 UUID／视频 URL 隔离保存任务，防止重复入队和旧回调覆盖最新媒体状态或提示。
- `MediaRecoveryStore`：在当前进程内保留未成功完成照片的保存操作，持久化失败视频路径，并为 failed + saving 视频提供去重容量闸门。
- `CameraNoticePolicy`：按恢复优先级保护尚未处理的可操作提示，并在用户点击时按该条提示的唯一 ID 消费入口。
- 最近照片／视频缩略图、保存状态徽标和保存失败重试入口。
- 较早媒体保存失败的可操作提示条，可直接选择“重试保存”。
- 视频准备／录制／处理状态，以及 `MM:SS`／`HH:MM:SS` 录制时长。

### 变更

- 将拍照改为具备 UUID、期望两路结果与超时清理的事务。
- 预览与成片改用同一布局引擎；修复默认画中画位置被错误约束到左上角的问题。
- 照片合成后改为自动在独立队列保存，不再自动跳转全屏结果页，保存中可继续实时取景和拍摄。
- 待保存照片在当前进程中限制为 5 个；达到上限时先提示处理旧任务，不再继续积累完整照片数据。
- 同一媒体的保存互斥保持到 PhotoKit 真实回调，不以本地超时提前释放，避免底层任务不可取消时重复写入。
- 可操作错误不再被迟到的普通成功／信息提示覆盖，且执行按钮后只消费当前 action 匹配的提示。
- 视频停止后立即恢复照片 Session；写入器完成后才在后台自动保存，不因 PhotoKit 写入暂停取景。
- 照片／视频查看页改为用户主动从缩略图进入，查看状态不再作为 Session 启动否决条件。
- 视频缓存新增 saving/saved/failed 显式状态：新媒体替换仍在保存的旧视频时，延迟到成功回调后再删除临时 `.mov`。
- 以 Session 队列单调序号维持媒体拍摄顺序；录像写入期间的更新照片不再被较晚完成的旧视频覆盖。
- 被替换视频只在保存成功后删除；失败时保留唯一 `.mov`，并在替换时或重新进入 active 后重试。
- failed 与 saving 视频按标准化 URL 去重；开始录像时，已有 5 个或现存文件累计 1 GiB 会阻止新录像并提供“重试保存”，不通过静默删除腾出容量。
- 正常退出相机界面时按视频保存状态收尾：已保存缓存立即清理，保存中缓存等待成功回调，失败缓存保留恢复索引。
- 中断、后台和媒体服务重置的恢复逻辑得到增强；媒体服务重置时清空录制授权等待与计时，但旧 writer 已在 `finishWriting` 时保持 finishing 闸门至回调，不提前发布 idle。
- 快速／均衡模式在 Session 启动前设置 PhotoOutput 上限和单次请求值，超出系统上限时安全钳制。
- MultiCam 配置改为有限尝试 30fps／24fps 候选，只有硬件和压力成本均不超过 1 才接受；serious／critical／shutdown 压力执行分级保护。
- inactive 不再等同 background；相机／麦克风授权回调会重新检查运行意图、active/background 和中断状态。
- 画中画拖动中不再逐帧写 UserDefaults 或重设镜像；后摄缩放以手势起始倍率计算。
- “前后原图”改为优先保存 `fileDataRepresentation()`；前摄预览、合成镜像和单路相机文件方向分离。
- 照片、视频和媒体保存错误改为独立状态与类型化用户操作，麦克风拒绝或单次媒体失败不破坏拍照 Ready。
- 保留固定画中画视频，补充幂等停止、无帧错误、临时 `.mov` 清理和播放器释放。

### 限制

- 双路照片为近同步采集，非严格硬件同步曝光。
- 双路视频使用最近可用前摄帧合成，同样不承诺逐帧硬件同步。
- 本轮后续真机验收尚未逐项完成，详见 `docs/真机验收.md`。
- 本分支尚未取得 GitHub Actions 远端成功记录。
