# 开发计划

- [x] 确认工作区、Xcode 和 AVFoundation MultiCam API 可用性。
- [x] 创建 SwiftUI iOS 工程及相机权限配置。
- [x] 通过 `AVCaptureMultiCamSession` 配置前摄与后置广角摄像头的独立预览、并发拍照和合成预览。
- [x] 完成 Swift 类型检查、`Info.plist` 校验、iPhoneOS 完整构建及真机验证项记录。
- [x] 在 iPhone 16 Pro 上完成前后摄实时预览、签名、安装与启动验收。
- [x] 完善说明、初始化 Git 并发布至 GitHub 私有仓库：`taoking/dual-camera-ios`。
- [x] 新增 iOS App 从账号申请、开发、签名、打包、安装、测试分发到 App Store 发布的全流程文档。

## 第二阶段：媒体能力与品牌资产

- [x] 添加原创 App Icon，并接入 iOS Asset Catalog。
- [x] 完善双摄合成拍照：照片预览、手动保存到系统相册与状态反馈。
- [x] 新增前后摄画中画视频录制、录音、视频预览与保存到系统相册。
- [x] 新增后置超广角／广角／长焦选择，仅展示可与前摄并发的镜头。
- [x] 完成 Swift 类型检查、资源校验和 iPhoneOS 无签名构建。
- [x] 修复真机拍照时请求超出双摄输出上限的质量，导致应用崩溃并显示白屏的问题。
- [x] 为照片和视频保存加入“保存中”禁用态与“已保存到相册”确认态，避免重复写入。
- [x] 完成第二阶段媒体预览与前后台恢复流程；其中“查看媒体时暂停会话”已在第六阶段被非阻断预览取代。
- [ ] 在 iPhone 16 Pro 上回归验证拍照保存、镜头切换、视频录制与视频保存。
- [x] 更新 README 与使用说明，补充图标、媒体权限、录制与镜头选择操作。
- [ ] 完成真机回归后提交并推送 GitHub。

## 第三阶段：可用性、布局与稳定性

- [x] 拆分相机模型、布局合成、相册保存、日志与视图模型职责。
- [x] 实现合成图／前后摄原图保存、系统分享与相册权限引导。
- [x] 实现画中画、左右分屏、上下分屏，且预览与成片使用同一布局引擎。
- [x] 实现画中画拖动、尺寸档位、位置与设置持久化。
- [x] 实现拍照事务、超时、非致命错误恢复、前后台与中断恢复、硬件诊断日志。
- [x] 实现后摄点击对焦／测光、缩放、网格、倒计时、震动与前摄镜像设置。
- [x] 新增布局／合成单元测试和 Fake Camera Mode。
- [x] 新增架构、真机验收、路线图与变更日志文档，并更新 README／使用说明。
- [x] 完成 iPhoneOS 构建与可用的自动化测试；真机验收项保持如实记录。

## 第四阶段：质量、架构与自动验收（待真机回归）

- [x] 审计质量模式、硬件成本、生命周期、原片保存和视频资源链路。
- [x] 拆分格式选择、拍照事务、生命周期与视频协调职责。
- [x] 修复质量配置、成本降级、系统压力、权限异步启动竞态。
- [x] 修复原始照片数据保存、画中画提交边界、对焦和缩放。
- [x] 补齐单元测试、Fake Camera UI 测试和自适应 GitHub Actions。
- [x] 更新架构、使用、路线图、变更记录和真机验收文档。
- [x] 完成本地模拟器测试、iPhoneOS 无签名构建及连接真机检查；初次 CLI 签名失败已记录，后续使用本机有效开发签名完成覆盖安装与启动。

## 第五阶段：发布、CI 与真机安装

- [x] 复核第四阶段改动范围，确认共享工程不包含个人 Team ID 或账号标识。
- [x] 核验最终结果包：64 个单元测试和 3 个 Fake Camera UI 测试全部通过。
- [x] 完成 iPhoneOS 无签名构建、静态分析、plist 与 diff 检查。
- [x] 提交并推送第四阶段改动，更新 Draft PR。
- [x] 等待并记录首轮 GitHub Actions 结果：单元测试通过，2 个顶部菜单 UI 用例因 Runner AX 点击兼容失败。
- [x] 使用本机签名在 iPhone 16 Pro 覆盖安装、启动新包。
- [x] 实现 iOS 18.5 Runner 的 SwiftUI Menu 点击兼容修复。
- [ ] 推送最新代码并重跑 GitHub Actions。
- [ ] 记录自动验收结果；保留需真人操作的真机项目为待办。

## 第六阶段：非阻断媒体保存与录制反馈（进行中）

- [x] 照片合成完成后自动后台保存，保存与结果提示均不停止实时双摄预览。
- [x] 视频停止后立即恢复照片取景，文件完成写入后自动后台保存且不暂停 Session。
- [x] 按媒体 ID 隔离并发保存状态，避免重复保存、旧任务覆盖新结果及视频临时文件提前删除。
- [x] 按拍摄单调序号保持最近媒体顺序，避免 finishing 视频较晚完成时覆盖更新照片。
- [x] 保存失败的被替换视频保留临时文件，在替换当下或再次进入 active 时重试，成功后才删除。
- [x] 将最近媒体改为缩略图入口，仅在用户主动点击时打开照片／视频查看页。
- [x] 显示录制准备、录制中、视频处理中状态，并以单调时钟持续显示录制时长。
- [x] 更新 Fake Camera UI 测试，覆盖预览不中断、失败重试、录制状态及时长。
- [x] 修复 iOS 18.5 Runner 顶部 SwiftUI Menu 点击兼容问题。
- [x] 更新 README、使用说明、架构说明、变更记录、路线图与真机验收清单。
- [x] 更新执行日志并封口本轮自动验收检查点。
- [x] 完成功能检查点的 67 个单元测试、3 个 Fake Camera UI 测试与 iPhoneOS 无签名构建。
- [x] 对检查点后的可控保存延迟、无障碍状态、拍摄顺序／失败视频恢复和文案收尾改动重跑完整自动验收：68 个单元测试、3 个 Fake Camera UI 测试及 iPhoneOS 无签名构建全部通过。
- [x] 使用本机有效开发签名完成第六阶段包构建、覆盖安装、添加信任后启动，并确认应用进程驻留。
- [x] 修复提交前审查发现的保存完成／文件删除竞态、控制器释放恢复、较早照片失败重试与媒体服务重置录像状态问题。
- [x] 以提示唯一 ID／媒体任务 ID 和恢复优先级修复错误提示覆盖、旧按钮误清及成功后残留问题；查看页和 Session 重试覆盖全部待恢复媒体。
- [x] 固化照片拍摄时的保存模式，避免拍摄后改设置导致查看页文案或手动重试保存内容不一致。
- [x] 实现视频 `inFlight → ready → PhotoKit` 两阶段恢复索引、冷启动 MOV 封口判定、进程首次孤儿扫描、5 分钟门槛与锁内删除复核。
- [x] 为媒体任务强持有、完成闸门、容量竞态、提示身份、两阶段恢复和孤儿缓存补充单元测试；当前源码预计 83 项。
- [x] 对当前最终源码重新执行 83 项单元测试、3 项 Fake Camera UI 测试、iPhoneOS 无签名／签名构建与真机覆盖安装；用户显式信任后已确认真机启动。
- [x] 新增可独立执行的开发签名续期与真机更新手册，涵盖账号、签名、构建、覆盖安装、信任、排障及最小验收。
- [x] 使用匹配 iOS 27 SDK 的 Xcode 27 Beta 重新签名、覆盖安装并启动 iPhone 16 Pro 上的当前开发包；记录多 Xcode 环境与信任处理。
- [ ] 提交并推送当前分支、更新 Draft PR，等待并记录 GitHub Actions 结果。
- [ ] 在 iPhone 16 Pro 完成照片／视频不停止取景、录制时长、后台保存及失败重试的真机回归。

## 第七阶段：画质、录像体验与工程健康度（2026-09）

- [x] 完成代码审核，输出 `docs/优化计划.md` 并分五个批次实施。
- [x] 批次 0：定位 iOS 27 工具链（本机默认 Xcode 不含 iOS 27 SDK），修正失效的测试命令，建立回归基线。
- [x] 批次 1：照片分辨率由 810×1440 提升到 2268×4032；修复真机专属的 `setMaxPhotoDimensions` 顺序崩溃。
- [x] 批次 2：录像接入布局引擎实现所见即所得；长边提到 1920、改 HEVC；帧合成移出会话队列。
- [x] 批次 3：缓存已验收格式组合降低起录延迟；重建时保留缩放。
- [x] 批次 4：补光、曝光补偿、AE/AF 锁、等效倍率读数、模式切换、查看页手势、倒计时可取消。
- [x] 批次 5：抽出 `CameraNoticeCenter` 与 `RecentMediaState`；并发检查升到 `targeted`。
- [x] 新增 `scripts/install-to-device.sh`，一条命令完成 7 天签名续期与覆盖安装。
- [x] 重做 App Icon 并改为脚本生成，输出常规／深色／单色三份。
- [x] 单元测试 83 → 129，UI 测试 3 → 5，零编译警告。
- [x] 整理 `docs/开发记录.md` 与更新后的 `docs/真机验收.md`。
- [ ] 按 `docs/真机验收.md` 逐条完成剩余真机项（当前 18/95 已确认）。
