# iPhone 16 Pro 真机验收

状态说明：自动测试与真机测试分开记录。模拟器或无签名构建成功不能替代这些结论。
请使用 Debug 真机包、开启控制台日志，并记录设备、iOS、Xcode、格式候选与异常截图。

勾选项后标注 `（2026-09-05 已确认）` 的，是本轮改动后由用户在 iPhone 16 Pro（iOS 27.0）
上实际操作确认的；未勾选项仍待逐条执行。本轮改动范围与背景见 [开发记录](开发记录.md)。

安装真机包：`./scripts/install-to-device.sh`（开发签名约 7 天到期，到期重跑同一条命令）。

## 基础与权限

- [ ] 冷启动，首次允许相机权限，前后预览同时出现。
- [ ] 拒绝相机权限后，状态正确提示并可从系统设置恢复。
- [ ] 首次录制时允许／拒绝麦克风，拍照不受拒绝麦克风影响。
- [ ] 首次拍照或完成视频触发自动保存时允许“添加照片”，相册中可找到对应媒体。
- [ ] 拒绝“添加照片”后，保存失败不退出，点击“去设置”能打开本 App 设置页。
- [ ] 重新授权相册后，不重装 App 也可再次保存。
- [ ] 请求相机或麦克风权限期间切到后台；授权回调返回时不会在后台错误启动 Session，返回前台后可恢复。

## 双摄与镜头

- [ ] iPhone 16 Pro 上同时显示真实前后摄预览，无单摄伪降级。
- [ ] 后置菜单只显示系统允许的镜头组合。
- [ ] 分别切换超广角、广角、长焦（若系统可用），预览恢复且没有崩溃。
- [ ] 拍照或录制中切镜头被安全阻止并给出提示。
- [ ] 设置菜单诊断区可读取：取景格式、**照片尺寸**、帧率、`hardwareCost`、`systemPressureCost`。
- [ ] 冷机默认取得 30fps 且**照片尺寸为该格式可请求的上限**；记录实际前后格式与两项成本，确认都 `<= 1`。
- [ ] 录像模式下格式以 1920×1080 为目标（成片统一合成到长边 1920 的画布）。
- [ ] 制造成本较高的镜头／模式组合时能有限降级；如出现 30fps → 24fps，保存完整候选尝试日志且没有无限重建。
- [ ] **格式缓存生效**：第二次及以后起录、切镜头、改质量时，日志中的候选尝试只有 1 轮而非多轮；等待时间明显短于首次。
- [ ] 全部候选都不达标时（例如高温）缓存被清除，不会持续优先尝试同一组失效组合。
- [ ] 每个系统允许的后置镜头分别完成一次拍照和一次视频录制。

## 布局与成片

- [ ] 画中画默认在右下，拖动后不越界，松手吸附最近角落。
- [ ] 画中画小／中／大三档尺寸正确，重启 App 后位置和尺寸保持。
- [ ] 左右分屏：后摄在左、前摄在右，预览与成片一致。
- [ ] 上下分屏：后摄在上、前摄在下，预览与成片一致。
- [ ] 3:4、1:1、9:16 三种比例均能完成拍照，成片尺寸与选择一致。
- [x] **成片分辨率跟随源图**：9:16 实测 2268×4032（约 9.1MP、2.5MB）。（2026-09-05 已确认）
- [ ] 切到 1:1 画幅拍照，成片**不出现上采样**：画布不超过源图短边所能覆盖的范围（3024×4032 源图应得 3024×3024，而非 4032×4032）。
- [ ] 三种画幅分别导出成片，用「文件」或相册信息核对像素尺寸与文件大小符合预期。
- [ ] 切换前摄预览镜像与成片镜像，效果分别生效。
- [ ] 保存的前摄单路相机文件保持自然方向；预览镜像和合成镜像互不串扰。

## 补光、曝光与对焦锁定

- [x] 顶部出现补光按钮（设备报告手电筒可用时才显示）。（2026-09-05 已确认）
- [x] 开启补光后手电筒真实点亮，拍照与录像期间保持常亮。（2026-09-05 已确认）
- [x] 退出相机界面后手电筒熄灭，不会留在开启状态。（2026-09-05 已确认）
- [x] 长按后摄画面出现黄色「AE/AF 锁定」徽标，画面对焦与测光被锁住。（2026-09-05 已确认）
- [x] 右侧曝光补偿滑杆拖动即时改变画面亮度。（2026-09-05 已确认）
- [ ] 再次长按解除锁定，恢复连续自动对焦与测光。
- [ ] 锁定状态下点按画面重新选点，锁定与曝光补偿一并复位。
- [ ] 切换镜头、改质量、起录停录后，补光状态按预期保持或复位；曝光补偿范围随新设备刷新。
- [ ] 暗光环境下开启补光完成一次拍照与一次录像，成片亮度符合预期。

## 缩放

- [x] 超广角下读数显示 **0.5×**，与镜头菜单标注一致（此前显示 1.0×）。（2026-09-05 已确认）
- [ ] 广角显示 1.0×，长焦显示该机型的等效倍率；三颗镜头读数与菜单标注不矛盾。
- [ ] 双指放大到上限，读数最高约 12×（此前为 6×），超出光学范围后画质下降属预期。
- [x] 放大后按下录制键，画面**保持当前倍率不弹回 1×**。（2026-09-05 已确认）
- [ ] 切换镜头后缩放重置为该镜头的 1×（这是预期行为，不是缺陷）。

## 拍摄模式与主键

- [x] 底部为「照片／视频」模式切换加单一主键。（2026-09-05 已确认）
- [x] 主键在照片模式为白色圆、视频模式为红色圆、录制中为红色方块。（2026-09-05 已确认）
- [x] 录制中模式切换隐藏，主键承担停止。（2026-09-05 已确认）
- [x] 出现错误提示条时**仍能点到模式切换**（提示条不遮挡）。（2026-09-05 已确认）
- [ ] 拍摄进行中或倒计时中不允许切换模式。

## 拍照体验与媒体

- [ ] 后摄点击对焦／测光有焦点框且对焦生效；点前摄不改变后摄焦点。
- [ ] 后摄双指缩放可用并保持在安全范围；前摄窗口捏合仅调整画中画尺寸。
- [ ] 网格开关和 3／5／10 秒倒计时生效，快门有触感反馈。
- [x] 倒计时期间点按屏幕任意处即取消，且不产生任何媒体。（2026-09-05 已确认）
- [ ] 倒计时期间再次点按主键同样表示取消，而不是叠加一次新的拍摄。
- [ ] 快速模式拍照成功，日志中的前后 output max/request 均为安全值，成片后可继续拍。
- [ ] 均衡模式拍照成功，系统允许时请求 `.balanced`，不因超过任一路 max quality 崩溃。
- [ ] 连续拍照 20 次：实时预览持续、每次自动保存并更新最近媒体，没有永久“拍摄中”状态，内存没有持续增长。
- [ ] 人为遮挡／中断一次拍照，超时或失败后可再次拍照。
- [x] 查看页有淡入缩放转场，照片支持双指缩放、双击放大、放大后拖动、下滑关闭。（2026-09-05 已确认）
- [ ] 放大到上限后拖动，图片不会被拖出可视范围。
- [ ] 从最近照片打开分享只展示系统分享面板，不会造成相册重复写入。
- [ ] 默认“仅保存成片”只写入一张；“成片和前后原图”一次写入三张。
- [ ] 导出两张单路照片的文件信息，确认优先保存相机原始文件 Data；若日志显示 JPEG 回退，记录机型、格式和原因。
- [ ] 照片保存中实时预览和下一次快门保持可用；保存成功只出现一次提示，失败可从最近媒体重试且不重复入队。

## 生命周期、压力与已有视频

- [ ] 拍照进行中切后台，回前台后无卡死、无永久拍摄状态。
- [ ] 打开控制中心或系统权限面板产生 inactive 时，不错误停止／重建 Session；返回后预览连续。
- [ ] 锁屏后解锁，Session 按状态恢复且旧拍照／录制结果不串入新事务。
- [ ] 会话中断（来电、控制中心或相机占用）后提示正确，恢复后可继续拍照。
- [ ] 系统恢复媒体服务后，会话能重建或给出可操作错误。
- [ ] 连续预览／拍照 10 分钟，观察日志和压力成本；serious 有提示并在支持时降 24fps，critical 停止／禁止录制，shutdown 安全停止。
- [ ] 拒绝麦克风后立即连续拍照 3 次，照片链路保持 Ready。
- [ ] 视频录制中触发来电／相机占用／切后台，录制只结束一次，不崩溃，返回后能处理结果或明确失败。
- [ ] 录制开始、录制中和停止处理状态准确；`MM:SS` 时长连续递增，交互期间不暂停。

### 录像所见即所得（本轮重点改动）

- [x] 把画中画拖到不同角落后录制，**成片位置跟随拖动**（此前固定在左下角，无视用户布局）。（2026-09-05 已确认）
- [x] 左右分屏、上下分屏均可录制，成片布局与预览一致（此前完全不支持）。（2026-09-05 已确认）
- [x] 3:4 与 1:1 画幅录制成功，实测分辨率分别为 1440×1920、1920×1920。（2026-09-05 已确认）
- [x] 录音正常（音频参数改用采集端推荐值）。（2026-09-05 已确认）
- [ ] 9:16 画幅录制，成片为 1080×1920。
- [ ] 成片编码为 HEVC，用「文件」或播放器信息核对；码率随画幅变化。
- [ ] 画中画大小三档在成片中分别生效。
- [ ] 前摄成片镜像开关对录像同样生效（与照片一致）。
- [ ] 录制中拖动画中画，成片按**起录时**的布局渲染（录制期间的布局变更不影响本段）。
- [ ] 遮挡前摄使其输出停顿，画中画区域**不会持续贴着一张过期画面**（超过 0.2 秒即只画后摄）。
- [ ] 录制期间点击对焦、双指缩放响应及时，不因逐帧渲染而卡顿（帧合成已移出会话队列）。
- [ ] 连续视频录制 5 次，每次停止后照片预览立即恢复，后台保存期间仍可拍照，音画可播放且前后帧近同步限制可接受。
- [ ] 最近视频的缓存 `.mov` 可供查看和失败重试；被新媒体替换时在对应保存任务结束后清理，无帧失败也无残留。
- [ ] 录制中打开控制中心再返回，in-flight writer 不会被误当作待保存视频提交或删除，录制继续且最终成片可播放。
- [ ] 在录制中、`finishWriting` 中和后台保存中分别强制结束 App 后重启：未封口文件被安全清理，已封口文件进入恢复保存，不永久占用 5 个／1 GiB 阈值。
- [ ] 快速离开再进入相机界面、或连续重建 Controller 时，另一个 writer／保存任务不被孤儿清理误删。
- [ ] 制造一个超过 5 分钟的未索引 `DualCamera-*.mov` 后重启，确认只清理真正孤儿；恢复索引、活动保存和当前预览文件均保留。
- [ ] 长时间录制时观察缓存大小与设备可用空间；当前 1 GiB 只检查开始录像前已有待恢复文件，不是单段录像硬上限，空间不足必须明确失败且不崩溃。
- [ ] 完成／关闭视频后立即拍照，照片模式、质量和前后输出正确恢复。
- [ ] 视频预览关闭后播放器停止，文件不再被占用。

## 自动验收记录模板

每次发布前单独记录下列命令的日期、Xcode/SDK、目标模拟器、测试总数、通过／失败／跳过数和 warning；GitHub Actions 只有出现远端成功记录后才能勾选 CI。

```sh
# 本机默认 xcode-select 指向的 Xcode 不含 iOS 27 SDK，必须先指定 Beta 工具链
export DEVELOPER_DIR=~/Downloads/Xcode-beta.app/Contents/Developer

plutil -lint DualCamera/Info.plist
xcodegen generate
git diff --check
xcodebuild test -project DualCamera.xcodeproj -scheme DualCamera \
  -destination '<实际可用模拟器>' -only-testing:DualCameraTests CODE_SIGNING_ALLOWED=NO
xcodebuild test -project DualCamera.xcodeproj -scheme DualCamera \
  -destination '<实际可用模拟器>' -only-testing:DualCameraUITests CODE_SIGNING_ALLOWED=NO
xcodebuild -project DualCamera.xcodeproj -scheme DualCamera \
  -sdk iphoneos -configuration Debug CODE_SIGNING_ALLOWED=NO build
```

## 2026-07-27 第四阶段初次本地自动验收结果（历史记录）

- 环境：macOS 27.0（26A5388g）、Xcode 26.6（17F113）、iOS Simulator 26.5，iPhone 17 Pro 模拟器。
- `plutil -lint`、`xcodegen generate`、`git diff --check`：通过。
- `DualCameraTests`：64 项通过、0 失败、0 跳过；最终结果包 `/tmp/dualcamera-unit-final3-20260727.xcresult`。
- `DualCameraUITests`：3 项通过、0 失败、0 跳过；最终结果包 `/tmp/dualcamera-ui-final2-20260727.xcresult`。Xcode 输出 LLDB `DebuggerVersionStore` 环境提示，但未导致测试失败。
- iPhoneOS Debug、`CODE_SIGNING_ALLOWED=NO`：构建成功；仅有 xcodebuild 在未指定唯一 destination 时选择首个目标的提示，没有 Swift 编译 warning。
- 当时 GitHub Actions 尚未运行，且首次命令行签名因账号／描述文件缺失而失败；后续发布、安装和 CI 首轮结果另见 `执行日志.md`。

## 2026-07-27 第六阶段本地自动验收结果

- `DualCameraTests`：68 项通过、0 失败、0 跳过；最终结果包 `/tmp/dualcamera-unit-nonblocking-final2-20260727.xcresult`。
- `DualCameraUITests`：3 项通过、0 失败、0 跳过；最终结果包 `/tmp/dualcamera-ui-nonblocking-final2-20260727.xcresult`。覆盖拍照后预览可用、最近媒体、后台保存状态、失败重试、录制状态和时长。
- iPhoneOS 无签名构建成功；本机开发签名构建和签名校验成功。
- 第六阶段包已覆盖安装到连接的 iPhone 16 Pro。首次启动因设备尚未显式信任该开发者被拒；用户添加信任后启动成功，并确认应用进程驻留。
- 上述结果只验证构建、自动测试、签名、安装与启动，不验证真实 MultiCam、PhotoKit 最终文件、真实麦克风音画或系统压力；代码仍需推送并取得远端 CI 结果，所有真人操作项继续保持未勾选。

## 应用图标

- [ ] 桌面图标为浅天蓝底的白色光圈环加右下角画中画角标。
- [ ] 深色模式下图标切换为深色变体（透明背景由系统合成），图形清晰。
- [ ] 单色主题下图标切换为单色变体，图形轮廓完整。
- [ ] 在文件夹、搜索结果、设置列表等小尺寸场景下，环、内芯、角标及其小点仍可分辨。
      若觉得白色配浅蓝发虚，加深 `DesignAssets/make-app-icon.swift` 中的渐变末端即可，图形无需改动。

## 2026-09-05 本轮自动验收结果

- 环境：macOS 27.0（26A5425a）、**Xcode 27.0（27A5228h）／iOS 27.0 SDK**、iPhone 17 Pro 模拟器（iOS 27.0）。
  本机另有 Xcode 26.6，仅含 iOS 26.5 SDK，不能构建 iOS 27 目标。
- `plutil -lint`、`xcodegen generate`、`git diff --check`：通过。
- `DualCameraTests`：**129 项**通过、0 失败、0 跳过（起点为 83 项）。
- `DualCameraUITests`：**5 项**通过、0 失败、0 跳过（起点为 3 项）。
- iPhoneOS Debug 无签名构建：成功，**零源码警告**，且已在 `SWIFT_STRICT_CONCURRENCY: targeted` 下。
- 开发签名构建、覆盖安装、启动：成功，进程驻留已确认；经 `scripts/install-to-device.sh` 完成。
- 上述结果只验证构建、自动测试、签名、安装与启动。真实 MultiCam 成本值、镜头组合、
  系统压力回调、麦克风音画同步、PhotoKit 最终文件方向仍以本文各项人工检查为准。

验收完成后，应把设备型号、iOS 版本、Xcode 版本、每项结果和 OSLog 导出附在 issue 或发布记录中。
